# Chetverka (Четверка)

"Четверка" — это iOS-приложение для учеников и их родителей, которое предоставляет удобный доступ к электронному дневнику сайта `schools.by`. Проект состоит из двух основных частей:

1.  **iOS-приложение (клиент)**: Написано на Swift с использованием SwiftUI.
2.  **Python-сервер (бэкенд)**: Написано на FastAPI и выступает в роли API-посредника, который "на лету" парсит данные с сайта `schools.by`.

## 🚀 Основные возможности

*   **Авторизация**: Безопасный вход в аккаунт `schools.by`.
*   **Просмотр дневника**: Отображение расписания, домашних заданий и оценок по неделям.
*   **Аналитика**: Наглядные диаграммы и графики для анализа успеваемости.
*   **Симуляция оценок**: Возможность спрогнозировать итоговые оценки, добавляя новые.
*   **Профиль ученика**: Просмотр данных профиля, включая ФИО, класс и фото.

## 🛠️ Технологический стек

### Frontend (iOS)

*   **Язык**: Swift
*   **UI Framework**: SwiftUI
*   **Архитектура**: MVVM (Model-View-ViewModel)
*   **Навигация**: `NavigationStack`
*   **Асинхронность**: `URLSession` для сетевых запросов.
## 🖥️ Установка и запуск


### Frontend

1.  **Откройте проект в Xcode:**
    Откройте файл `Chetverka.xcodeproj` в Xcode.

2.  **Настройте URL бэкенда:**
    В файле `Chetverka/Services/DiaryAPI.swift` измените значение константы `baseURL` на IP-адрес вашего компьютера в локальной сети (например, `http://192.168.1.5:8000`).

3.  **Запустите приложение:**
    Выберите нужный симулятор или реальное устройство и нажмите "Run" (▶).

### Android (Kotlin + Compose)

1.  **Откройте Android-проект в Android Studio:**
    Откройте папку `android-app`.

2.  **Синхронизируйте Gradle:**
    Нажмите `Sync Project with Gradle Files`.

3.  **Проверьте URL бэкенда:**
    В `android-app/app/build.gradle.kts` используется:
    - debug: `http://10.0.2.2:8000/` (эмулятор Android)
    - release: `http://192.168.0.113:8000/` (физическое устройство в локальной сети)

4.  **Запустите бэкенд и Android-приложение:**
    - Бэкенд: `uvicorn app:app --host 0.0.0.0 --port 8000`
    - Android: кнопка Run в Android Studio.

## API Эндпоинты

*   `POST /login`
    *   **Тело запроса:** `{ "username": "user", "password": "password" }`
    *   **Ответ:** `{ "sessionid": "...", "pupilid": "...", "profile": { ... } }`

*   `GET /parse`
    *   **Query параметры:** `sessionid`, `pupilid`
    *   **Ответ:** `{ "weeks": [ ... ] }` (структурированный JSON с данными дневника)
